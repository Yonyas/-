최소한의 동전 개수로 

```js
const change = money => {
  let answer = 0;
  const changes = [500, 100, 50, 10];
  
  changes.forEach(v => {
    if (money >= v) {
      answer += Math.floor(money/v)
      money -= Math.floor(money/v)*v
    } 
  })      
  return answer
}
```
